<%= form_for(@membership) do |f| %>
  <% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :swap_id %><br>
    <%= f.collection_select :swap_id, Swap.all, :id, :swap_description, prompt:true %>
  </div>
<!--  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_area :user_id %>
  </div>

  <div>
      <%=@user%>
    </div>
  -->

    <div class="field">
      <%= f.label :user_id %><br>
      <%= f.collection_select :user_id, User.all, :id, :email %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
