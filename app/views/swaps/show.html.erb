<p id="notice"><%= notice %></p>

<p>
  <strong>Location:</strong>
  <%= @swap.location %>
</p>

<p>
  <strong>When:</strong>
  <%= @swap.when %>
</p>

<p>
  <strong>Swap description:</strong>
  <%= @swap.swap_description %>
</p>

  <p>
  <strong>Users in this Swap:</strong><br>
    


 <%= render 'userlist' %> <br> <br>  </p>

<p>Click this button when everyone in your swap has joined to assign Secret Santas! <br>
   Each user will be able to see their assigned person when they log into the app.</p>
<%= button_to "Assign Secret Santas", autoassign_swap_path(@swap)%>

<%= render 'recipient' %>


    <strong>Your recipient's wishlist:</strong><br>
      <% @recipient.wishlist.each do |item| %>
        <%= item.description %>
        <%= item.link %>
        <br>
      <% end %>

<%= link_to 'Edit', edit_swap_path(@swap) %> |
<%= link_to 'Back', swaps_path %>

<ul>
  <% @swap.recipients.each do | recipient | %>
    <li><%= recipient.user %> => <%= recipient.recipient %></li>
  <% end %>
</ul>
